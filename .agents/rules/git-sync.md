# Git Auto-Push Rule

Whenever any modifications, bug fixes, or enhancements are made to this mod repository:
- Automatically stage (`git add .`), commit with a descriptive message, and push (`git push origin main`) to GitHub so the user can immediately test and update the mod via Sine / Zen Browser.
